
---
id: remove-duplicates-from-sorted-list
title: 83. Удаление дубликатов из отсортированного списка
difficulty: Easy
leetcode_url: https://leetcode.com/problems/remove-duplicates-from-sorted-list/description/
description: Дан отсортированный связный список. Удалить все дубликаты так, чтобы каждый элемент встречался только один раз. Вернуть отсортированный список.
---

**Подход: Один проход (прямолинейный обход)**

1. **Идея:** Так как список уже отсортирован, все дубликаты идут подряд. Проходим по списку один раз, и если текущий узел имеет то же значение, что и следующий, «перепрыгиваем» следующий узел, меняя указатель `next`.
2. **Логика:** Начинаем с `current = head`. Пока `current` и `current.next` существуют: сравниваем `current.val` и `current.next.val`. Если равны → `current.next = current.next.next` (не двигаем `current`, так как новый `current.next` тоже может быть дубликатом). Если не равны → `current = current.next`.
3. **Почему один проход:** Свойство отсортированности гарантирует, что дубликаты находятся в смежных узлах. Решение работает in-place без дополнительной памяти.
4. **Особые случаи:** Пустой список (`head == null`) → возвращаем `null`. Список из одного элемента → возвращаем без изменений.
5. **Время:** O(n), где n — количество узлов в списке. Один проход.
6. **Память:** O(1), модифицируем связи в самом списке.

**Ключевой момент:** После удаления дубликата (`current.next = current.next.next`) **не** двигаем `current` вперёд, потому что следующий узел может оказаться ещё одним дубликатом.
