---
id: sqrtx
title: 69. Квадратный корень из X
difficulty: Easy
leetcode_url: https://leetcode.com/problems/sqrtx/description/
description: Дано неотрицательное целое x. Вернуть целую часть квадратного корня. Бинарный поиск в диапазоне [0, x].
---
**Подход: Бинарный поиск**
1. **Идея:** Ищем наибольшее целое `mid`, такое что `mid * mid <= x`. Используем бинарный поиск в диапазоне `[0, x]` (или `[0, x/2 + 1]` для x > 1).
2. **Логика:** `left = 0`, `right = x`. Пока `left <= right`: `mid = left + (right - left) // 2`. Если `mid * mid == x` → вернули mid. Если `mid * mid < x` → двигаем left (ответ где-то справа), сохраняем mid как кандидата. Если `mid * mid > x` → двигаем right.
3. **Почему бинарный поиск:** Пространство поиска монотонно: если `a < b`, то `a² < b²`. Это позволяет отсекать половину диапазона за шаг.
4. **Особые случаи:** `x = 0` → `0`. `x = 1` → `1`. Большие числа — `mid * mid` может переполнить int, поэтому проверяем `mid <= x / mid`.
5. **Время:** O(log x) — бинарный поиск.
6. **Память:** O(1).
**Ключевой момент:** Для избежания переполнения условие `mid * mid <= x` заменяем на `mid <= x / mid`.
