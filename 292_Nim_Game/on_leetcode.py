class Solution:
    def canWinNim(self, n: int) -> bool:
        # Если n делится на 4 — проигрыш
        # Иначе — выигрыш
        # Пример: 4 → False (сколько ни бери, противник заберёт остаток)
        # 5 → True (берём 1, оставляем 4 противнику — он проиграет)
        return n % 4 != 0
